"use client";

import { HashLoader } from "react-spinners";

const RootLoagingSpinner = () => {
  return <HashLoader size={60} loading={true} speedMultiplier={1} />;
};

export default RootLoagingSpinner;
